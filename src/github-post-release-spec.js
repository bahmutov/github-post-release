'use strict'

/* global describe, it */
const githubPostRelease = require('.')

describe('github-post-release', () => {
  it('write this test', () => {
    console.assert(githubPostRelease, 'should export something')
  })
})
